# ============================================================
# 🚀 CONFIGURACIÓN DEL SERVIDOR
# ============================================================
server.port=9090

# ============================================================
# 🍃 CONFIGURACIÓN DE MONGODB
# ============================================================
# URI de conexión a MongoDB (local)
spring.data.mongodb.uri=mongodb://localhost:27017/gestionventas_nosql

# 👉 Explicación:
# - "mongodb://" → protocolo de conexión
# - "localhost" → tu servidor local
# - "27017" → puerto por defecto de MongoDB
# - "gestionventas_nosql" → nombre de la base de datos en Mongo (puedes cambiarlo)

# Si tu Mongo tiene autenticación habilitada, usa este formato:
# spring.data.mongodb.uri=mongodb://usuario:contraseña@localhost:27017/gestionventas_nosql?authSource=admin

# ============================================================
# 🧠 CONFIGURACIÓN GENERAL DE SPRING
# ============================================================
# Evitar que Spring JPA intente inicializar Hibernate (ya no usamos SQL)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration, \
org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration, \
org.springframework.boot.autoconfigure.jdbc.DataSourceTransactionManagerAutoConfiguration

# ============================================================
# 🪶 LOGGING / DEBUG
# ============================================================
# Mostrar las operaciones que realiza Mongo en consola
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG
logging.level.org.springframework.data.mongodb.repository.Query=DEBUG

# ============================================================
# 🌐 CORS (si quieres mantener abierto en desarrollo)
# ============================================================
# Esto lo manejas ya en tu clase WebConfig.java

